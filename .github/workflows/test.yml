name: Test

on:
  push:
  pull_request:

jobs:
  deterministic-browser-core:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
      - run: npm test
      - run: npm run result -- 23
      - name: Verify Gate 1 frozen receipt stayed frozen
        run: git diff --exit-code -- results/seed23.json
      - run: npm run gate2 -- 23
      - name: Verify Gate 2 frozen receipt stayed frozen
        run: git diff --exit-code -- results/gate2-seed23.json
      - name: Run frozen Gate 3 seed 23
        run: npm run gate3 -- 23
      - name: Verify Gate 3 frozen receipt stayed frozen
        run: git diff --exit-code -- results/gate3-seed23.json
